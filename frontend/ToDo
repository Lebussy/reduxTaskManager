- Decide on color scheme

- Connect to mongo 

- Change the update of positions so that only the id of the task and the new position is sent- using a patch request
  - same with editing the content, change to a patch request