export const appStyle = {
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    textAlign: 'center',
    backgroundColor: '#cbd3db',
    width: 'fit-content',
    padding: '10px',
    maxHeight: '90vh', // Set a maximum height
    overflowY: 'auto' // Make it scrollable if content exceeds max height
  }

  export const buttonStyle = {
    color: 'black', // Set the text color
    backgroundColor: '#7399ff', // Set the background color
    border: '1px solid black', // Optional: remove border
    padding: '5px 10px', // Optional: add padding
    borderRadius: '3px', // Optional: add border radius
    cursor: 'pointer', // Optional: change cursor on hover
    margin: '2px'
  }

  export const tasksDisplayStyle = 
  {
    display: 'block', // Ensure the div is a block element
    textAlign: 'left', // Align text to the left
    border: '1px solid black',
    padding: '10px',
    backgroundColor: '#FFFFFF'
  }
